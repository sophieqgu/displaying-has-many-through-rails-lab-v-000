<bold><%= @patient.name %></bold> has the following appointmnents:
<ul>
<% @patient.appointments.each do |appointment| %>
  <li><%= appointment.datetime %> - <%= link_to appointment.doctor.name, doctor_path(appointment.doctor) %></li>
<% end %>
</ul>
